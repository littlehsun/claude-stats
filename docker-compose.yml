services:
  claude-stats:
    build:
      context: .
    ports:
      - "${PORT:-5050}:${PORT:-5050}"
    environment:
      - PORT=${PORT:-5050}
    volumes:
      - "${HOME}/.claude/projects:/root/.claude/projects:ro"
      - "${HOME}:/host-home:ro"
